---
name: Popup
menu: Util Components
route: /util-components/popup
---

import { Playground, PropsTable } from 'docz';
import Popup from './index';
import packageInfo from './package.json';
import { PackageInfo, Component } from '@docs';

# Popup

<PackageInfo {...packageInfo} />

## Usage

```js
import Popup from '@scarf/popup';
```

## Properties

<PropsTable of={Popup} />

## Uncontrolled Popup

<Playground>
  <Popup
    content={({ placement, close }) => (
      <div
        style={{
          padding: 10,
          marginTop: 10,
          border: '2px solid red',
          background: 'white'
        }}
      >
        Popup Content
        <button onClick={close}>Close</button>
      </div>
    )}
  >
    {({ ref, toggle, isOpen }) => (
      <button ref={ref} onClick={toggle}>
        Currently {isOpen ? 'open' : 'closed'}
      </button>
    )}
  </Popup>
</Playground>

## Controlled Popup

<Playground>
  <Component initialState={{ open: false }}>
    {({ setState, state }) => (
      <Popup
        isOpen={state.open}
        onToggle={open => {
          setState({ open });
        }}
        onOutsideClick={() => {
          setState({ open: false });
        }}
        content={({ placement, close }) => (
          <div
            style={{
              padding: 10,
              marginTop: 10,
              border: '2px solid red',
              background: 'white'
            }}
          >
            Popup Content
            <button onClick={close}>Close</button>
          </div>
        )}
      >
        {({ ref, toggle, isOpen }) => (
          <button ref={ref} onClick={toggle}>
            Currently {isOpen ? 'open' : 'closed'}
          </button>
        )}
      </Popup>
    )}
  </Component>
</Playground>

## Popup placement

The default placement is `bottom-start`, but you could override it with other positions.

Valid placements are:

- `auto`
- `top`
- `right`
- `bottom`
- `left`

Each placement can have a variation from this list:

- `-start`
- `-end`

Some valid examples are:

- `top-end` (on top of reference, right aligned)
- `right-start` (on right of reference, top aligned)
- `bottom` (on bottom, centered)
- `auto-end` (on the side with more space available, alignment depends by placement)

<Playground>
  <Popup
    placement="right"
    content={({ placement, close }) => (
      <div
        style={{
          padding: 20,
          marginLeft: 10,
          border: '2px solid red',
          background: 'white'
        }}
      >
        Popup Content
      </div>
    )}
  >
    {({ ref, toggle, isOpen }) => (
      <button ref={ref} onClick={toggle}>
        Currently {isOpen ? 'open' : 'closed'}
      </button>
    )}
  </Popup>
</Playground>
